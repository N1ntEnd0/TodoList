import { StyleSheet } from "react-native";

export default StyleForm = StyleSheet.create({
    container: {
        marginTop: 40,
        alignItems: 'center',
    },
    input: {
        width: "50%",
        borderBottomWidth: 1,
        fontSize: 16,
        textAlign: "center",
    },
});